# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# Virtualenv
.venv/
venv/
env/

# uv
uv.lock.bak

# pytest / coverage
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/

# mypy / ruff
.mypy_cache/
.ruff_cache/
.dmypy.json

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project — DuckDB single-file stores and raw ingest dumps
*.duckdb
*.duckdb.wal
data/raw/
data/*.duckdb
data/*.duckdb.wal

# Allow committed demo fixtures (small, real RIS data slice).
!data/fixtures/
!data/fixtures/*.duckdb

# Allow committed baselines (filtered RIB pulls, < 1 MB each).
!data/baselines/
!data/baselines/*.duckdb

# Secrets
.env
.env.local


# ML raw windows (large; rebuild via scripts/build_ml_dataset.py + docs/ml/README.md)
data/ml/*_unfiltered.duckdb
data/ml/legit_*.duckdb
