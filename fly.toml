# NetPulse on Fly.io
#
# Deploys the FastAPI surface bound to the bundled YouTube fixture +
# RIB baseline -- one HTTP service that returns real BGP detector
# alerts for the canonical incident. To deploy:
#
#     flyctl auth login
#     flyctl apps create netpulse        # one-time
#     flyctl deploy                      # builds Dockerfile, ships
#
# The default app name 'netpulse' may already be taken on Fly; pass
# --app=<your-name> to flyctl deploy to use something else, or rename
# the app key here.

app = "netpulse-pauti"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

  # Liveness: cheap, never blocks. Consecutive failures restart the
  # machine.
  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/health"

  # Readiness: opens the BGP DuckDB store. Failure routes traffic
  # away from the machine without restarting it -- useful when the
  # backing store has gone bad (truncated DB, missing fixture) so the
  # operator can investigate without a restart loop.
  [[http_service.checks]]
    interval = "60s"
    timeout = "10s"
    grace_period = "15s"
    method = "GET"
    path = "/ready"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512
