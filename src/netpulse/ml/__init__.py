"""Machine-learning layer: unsupervised anomaly detection over BGP windows.

Optional — requires the ``[ml]`` extra (scikit-learn, numpy). Import lazily
so the core package stays dependency-light.
"""
